{
  "label": {
    "enabled": true,
    "endpoints": {
      "${SERVICE_ENDPOINT}": {
      }
    },
    "port": ${SERVICE_PORT},
    "desc": ""
  },
  "name": "${SERVICE_NAME}",
  "serviceName": "${SERVICE_NAME}",
  "host": "${SERVICE_HOST}",
  "healthcheck" : "${SERVICE_HEALTHCHECK}"
}
